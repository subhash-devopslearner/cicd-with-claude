services:

  db:    
    ports:
      - "5433:5432"    
    
  redis:    
    ports:
      - "6380:6379"

  web:    
    ports:
      - "8001:8000"    
    command: gunicorn --chdir cicdproject cicdproject.wsgi:application --bind 0.0.0.0:8000 --workers 2

