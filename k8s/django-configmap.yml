apiVersion: v1
kind: ConfigMap
metadata:
  name: django-config
data:
  DEBUG: "True"
  ALLOWED_HOSTS: "localhost,127.0.0.1,minikube.local,django.local"
  DB_HOST: "web-service"
  DB_PORT: "5432"
  DB_ENGINE: "postgresql"
  ENVIRONEMRNT: "dev"